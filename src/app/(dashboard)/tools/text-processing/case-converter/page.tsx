import { Textarea } from "@/components/ui/textarea";
import React from "react";

const page = () => {
    return (
        <div>
            <Textarea label="Input text"/>
        </div>
    );
};

export default page;
